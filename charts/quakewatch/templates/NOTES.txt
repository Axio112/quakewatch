1) Get the application URL (NodePort):
   kubectl get svc {{ .Release.Name }}-helm -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[0].nodePort}'

   Windows + Minikube (Docker driver):
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Release.Name }}-helm 5000:80
   curl http://localhost:5000
